# Dementia dashboard

Static interactive dashboard for exploring dementia population attributable fractions across European SHARE countries.

The site is designed for GitHub Pages. It uses a client-side placeholder password (`corossol`) only as a presentation gate; this is not real security because all static files remain downloadable from the page source.
